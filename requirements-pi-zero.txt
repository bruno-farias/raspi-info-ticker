# Lightweight requirements for Raspberry Pi Zero W
# This file contains only essential packages optimized for limited resources

# Core essentials
python-dotenv==1.0.1
Pillow==10.4.0
requests==2.32.3
PyYAML>=6.0

# API clients
freecurrencyapi==0.1.0

# Configuration and validation (minimal)
pydantic>=2.0
pydantic-settings>=2.0

# Async support (essential only)
aiohttp>=3.9
asyncio

# Simple authentication
passlib>=1.7.4
bcrypt>=4.0.1
aiohttp-session>=2.12.0

# For timezone support
pytz>=2023.3

# Optional - Install separately if needed:
# ------------------------------------------
# For SVG support (uses more memory):
# cairosvg>=2.5.0
# numpy>=1.21.0

# For TUI (not recommended on Pi Zero W):
# textual>=0.70.0
# rich>=13.0.0

# For web server (resource intensive):
# textual-serve>=1.0.0
# uvicorn>=0.30.0
# python-multipart>=0.0.9

# For advanced auth:
# authlib>=1.3
# python-jose[cryptography]>=3.3.0
# aiosqlite>=0.20.0